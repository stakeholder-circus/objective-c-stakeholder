name: Scaffold Native CI

on:
  push:
  pull_request:

jobs:
  scaffold:
    runs-on: ubuntu-latest
    steps:
      - name: Scaffold-only status
        run: |
          echo "scaffold-only native CI placeholder; no deterministic runtime validation is claimed"
